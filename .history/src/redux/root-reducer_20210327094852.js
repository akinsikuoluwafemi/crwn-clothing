import { combineReducers } from 'redux';

import userReducer from './user/user.reducer';

export default combineReducers({
    user: const [state, dispatch] = useReducer(reducer, initialState, init)
})